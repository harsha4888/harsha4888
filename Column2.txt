Column = 
VAR MaxQuater = MAX('Viz Expenses'[quater])
RETURN
    IF(
        'Viz Expenses'[quater] = MaxQuater, 
        "Current Quarter",
        IF(
            'Viz Expenses'[quater] = MaxQuater - 1, 
            "Last Quarter",
            ""
        )
    )
