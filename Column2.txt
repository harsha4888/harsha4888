

Debug Column = 
IF(
    [date_filtered] = 0, 
    FORMAT('Viz Expenses'[Created Date], "YYYY-MM-DD"),
    IF(
        [date_filtered] = 1, 
        FORMAT('Viz Expenses'[Payment Date], "YYYY-MM-DD"),
        FORMAT('Viz Expenses'[Transaction Date], "YYYY-MM-DD")
    )
)
