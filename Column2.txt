color max new column = 
SWITCH(
    TRUE(),
    'Viz Expenses'[Avg_Trans] >= 27, "RED",  // Red for high-risk scores
    'Viz Expenses'[Avg_Trans] >= 1.66, "YELLOW",  // Yellow for mid-risk scores
    'Viz Expenses'[Avg_Trans] >= 0.80, "GREEN",  // Green for low-risk scores
    "GREEN"  // Default to green for low-risk
)

