Column = if('Viz Expenses'[quater]=max('Viz Expenses'[quater]),"Current Quarter",
  if(('Viz Expenses'[maxquater])<>"1" && 'Viz Expenses'[quater]=max('Viz Expenses'[quater])-1,"Last Quarter",
  if('Viz Expenses'[quater]=max('Viz Expenses'[quater])-7,"Last Quarter","")
  )
  )

