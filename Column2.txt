Quart Label = 
VAR MaxRank = MAXX(ALL('Viz Expenses'), 'Viz Expenses'[Quart Rank])
VAR LastQuarterRank = MaxRank - 1
RETURN
    SWITCH(
        TRUE(),
        'Viz Expenses'[Quart Rank] = MaxRank, "Current Quarter",
        'Viz Expenses'[Quart Rank] = LastQuarterRank, "Last Quarter",
        "Other"
    )
